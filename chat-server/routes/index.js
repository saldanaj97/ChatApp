import express from 'express'

/* Controllers */
import users from '../controllers/user.js'

const router = express.Router()

router.post('/login/:userid', encode, (re, res, next) => { });

export default router